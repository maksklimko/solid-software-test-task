name: solid_software_test_task
description: "Test task project developed with flutter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  custom_lint: ^0.6.7
  flutter_test:
    sdk: flutter
  solid_lints: ^0.2.3

flutter:
  uses-material-design: true